<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true">

    <EditText
        android:id="@+id/et_registration_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="64dp"
        android:ems="10"
        android:inputType="textPersonName"
        android:text="Name" />

    <CheckBox
        android:id="@+id/cb_need_password"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/et_registration_name"
        android:layout_alignStart="@+id/et_registration_name"
        android:layout_below="@+id/et_registration_name"
        android:layout_marginTop="8dp"
        android:text="need password?" />

    <EditText
        android:id="@+id/et_registration_password"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/et_registration_name"
        android:layout_alignRight="@+id/et_registration_name"
        android:layout_below="@+id/cb_need_password"
        android:ems="10"
        android:inputType="textPersonName"
        android:text="Password"
        android:visibility="gone"
        tools:visibility="visible" />

    <Button
        android:id="@+id/btn_create_account"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/et_registration_password"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Create" />
</RelativeLayout>